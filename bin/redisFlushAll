#!/usr/bin/env node

const redis = require('redis');
const client = redis.createClient();

client.flushdb((error, success) => {
  OLOG.log({ level: 'info', message: `Redis flush all: ${success}` });
  client.quit();
});
